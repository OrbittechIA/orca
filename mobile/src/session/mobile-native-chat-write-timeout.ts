// A leaf so structured-session callers (the Tasks page's Work Item Start) can share the floor
// without bundling the native chat terminal-write path.
export const MOBILE_NATIVE_CHAT_MIN_WRITE_TIMEOUT_MS = 2_000
